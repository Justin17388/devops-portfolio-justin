apiVersion: v1
kind: Service
metadata:
  name: flask-hello
  labels:
    app: flask-hello
spec:
  type: ClusterIP
  selector:
    app: flask-hello
  ports:
    - name: http
      port: 80
      targetPort: 5000
